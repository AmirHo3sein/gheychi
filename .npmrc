# Windows MAX_PATH workaround: pnpm's default 120-char virtual-store folder
# names can push nested package paths (e.g. @nuxt/vite-builder) past the
# 260-char limit on deep clone paths.
virtual-store-dir-max-length=60
